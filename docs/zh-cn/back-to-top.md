# 回到顶部按钮

浮动回到顶部按钮，便于页面导航。

## 配置

```toml
[params.features]
  backToTop = true

[params.backToTop]
  scrollPercent = true
```

## 用法

按钮在页面向下滚动后自动出现。当 `scrollPercent` 启用时，它会显示滚动百分比。

## 相关链接

- [主题配置](../en/theme-configuration.md)